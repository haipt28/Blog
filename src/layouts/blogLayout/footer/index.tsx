import { Stack } from '@mui/material';
import * as React from 'react';

export interface IFooterBlogProps {}

export default function FooterBlog(props: IFooterBlogProps) {
  return <Stack sx={{ background: 'green' }}>HeaderBlog</Stack>;
}
